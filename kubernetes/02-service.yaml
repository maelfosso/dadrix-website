apiVersion: v1
kind: Service
metadata:
  name: website
  namespace: dadrix
  labels:
    tiers: website
    app: website
spec:
  selector:
    tiers: website
    app: website
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
